import { Container } from 'react-bootstrap';

const RecruiterDashboard = () => (
  <Container className="py-5">
    <h1>Recruiter Dashboard</h1>
    <p>Not used - we only have 2 roles (jobseeker, admin)</p>
  </Container>
);

export default RecruiterDashboard;
